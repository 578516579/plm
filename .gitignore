# Claude Code session data — ignore everything under .claude/ EXCEPT shared rule/playbook .md files
.claude/*
!.claude/*.md

# Environment files with secrets
.env
.env.local
*.env.local

# OS / Editor
.DS_Store
Thumbs.db
*.swp
*~

# IDE (root-level fallback; subprojects have their own)
.idea/
.vscode/
