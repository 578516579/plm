# 测试计划 — PLM 已交付功能自动化测试 (v0.4)

| 字段 | 值 |
|---|---|
| 版本 | v0.4 |
| 测试周期 | 持续（CI 每次 push）+ 每模块 Phase 03→04 准入时 |
| 测试对象 | 13 个 PRD-aligned 已交付业务模块 + 跨切关注（编码/导航） |
| 框架 | Playwright 1.60+ (Chromium) — 详 [E2E-测试矩阵.md](测试用例库/E2E-测试矩阵.md) |
| 工具链 | [.claude/skills/plm-e2e](../.claude/skills/plm-e2e/SKILL.md) + `/e2e-run` / `/e2e-smoke` / `/e2e-encoding` / `/e2e-debug` |
| 当前规模 | **~91 case / 24 spec 文件** |

---

## 1. 测试范围

### 1.1 已交付功能（必须 100% 覆盖）

| # | 模块 | 后端 Maven 模块 | E2E spec | 覆盖深度 |
|---|---|---|---|---|
| 1 | Project | plm-project | [project.spec.ts](../plm-frontend/e2e/project.spec.ts) | 5 case — UI |
| 2 | Requirement | plm-requirement | [requirement.spec.ts](../plm-frontend/e2e/requirement.spec.ts) | 6 case — 状态机 + FK + UI |
| 3 | Sprint | plm-sprint | [sprint.spec.ts](../plm-frontend/e2e/sprint.spec.ts) | 6 case — 状态机 + 703 + stats |
| 4 | Task | plm-task | [task.spec.ts](../plm-frontend/e2e/task.spec.ts) | 10 case — 6×6 状态机 + 看板 |
| 5 | Defect | plm-defect | [defect.spec.ts](../plm-frontend/e2e/defect.spec.ts) | 8 case — 5×5 + 反向边 + 3 FK |
| 6 | TestCase | plm-testcase | [testcase.spec.ts](../plm-frontend/e2e/testcase.spec.ts) | 8 case — 5×5 + /execute + 706 |
| 7 | Document | plm-document | [document.spec.ts](../plm-frontend/e2e/document.spec.ts) | 8 case — 文档类型/版本/评审 |
| 8 | Submission | plm-submission | [submission.spec.ts](../plm-frontend/e2e/submission.spec.ts) | 5 case — 708 质量门禁 |
| 9 | Release | plm-release | [release.spec.ts](../plm-frontend/e2e/release.spec.ts) | 4 case — UK + 编码 + FK |
| 10 | TestPlan | plm-testplan | [testplan.spec.ts](../plm-frontend/e2e/testplan.spec.ts) | 3 case — CRUD + 编码 + FK |
| 11 | TestReport | plm-testreport | [testreport.spec.ts](../plm-frontend/e2e/testreport.spec.ts) | 4 case — 风险白名单 + 编码 |
| 12 | ApiDoc | plm-apidoc | [apidoc.spec.ts](../plm-frontend/e2e/apidoc.spec.ts) | 4 case — UK + 编码 + FK |
| 13 | ManualProduct | plm-manual-product | [manual-product.spec.ts](../plm-frontend/e2e/manual-product.spec.ts) | 3 case — 编码 + FK |

### 1.2 跨切关注

| 关注点 | spec | case 数 |
|---|---|---|
| **字符编码守门员（P0 必跑）** | [encoding.spec.ts](../plm-frontend/e2e/encoding.spec.ts) | 6 |
| 菜单导航烟雾测试 | [navigation.spec.ts](../plm-frontend/e2e/navigation.spec.ts) | 3 |

### 1.3 未交付/空壳模块

16 个空壳模块（competitive / prd / arch / dbdesign / apidesign / ued / testdata / autotest / manual-impl / manual-ops / analytics / dashboard / ai-agent / openspec / pipeline / feature-flag / dora）+ 1 个缺模块（inception）目前只有占位 spec（每个 1 case 验证 POST 返 200）。完整 E2E 覆盖等模块 PRD-align 落地完成后补。

---

## 2. 测试类型

| 类型 | 工具 | 当前状态 |
|---|---|---|
| ✅ **E2E 自动化** | Playwright | 已落地（91 case） |
| ✅ **接口测试** | Playwright APIRequestContext | 已落地（与 E2E 同一框架） |
| ✅ **字符编码回归** | Playwright + DB HEX 校验 | 已落地（encoding.spec.ts 6 case） |
| ⚠️ 单元测试（后端） | JUnit | 部分模块（plm-mcp 已落地） |
| ⚠️ 单元测试（前端） | Vitest | 未启用 |
| ⚠️ 性能测试 | (TBD JMeter / k6) | 见 [性能报告/](性能报告/) |
| ⚠️ 安全测试 | (TBD OWASP ZAP) | 见 [安全审计.md](安全审计.md) |
| ❌ 回归测试 | 同 E2E 全套件 | 通过 CI 每 push 跑 |

---

## 3. 测试环境

| 环境 | URL | 数据情况 | 维护人 |
|---|---|---|---|
| dev (本地) | http://localhost (前端) + http://localhost:8081 (后端) | 本地 MySQL + Redis | 开发者本人 |
| staging | TBD | 脱敏生产数据子集 | TBD |
| prod | TBD | 真实 | 仅 oncall |

**dev 环境前置**（详 [E2E-运行手册.md §1](测试用例库/E2E-运行手册.md)）:
- MySQL 8.0+ on 3306，schema=plm，utf8mb4
- Redis 7.x on 6379（注意：用 `127.0.0.1` 不要 `localhost`，IPv6 坑）
- JDK 17 + 后端启动带 `-Dfile.encoding=UTF-8` 等 4 个标志
- Node 20 + `npm install` + `npx playwright install chromium`
- 环境变量 `DB_PASSWORD` 已 export（[helpers/db.ts](../plm-frontend/e2e/helpers/db.ts) 会抛错）

---

## 4. 进入 / 退出标准

### 4.1 进入标准（Phase 03 → 04 准入 — [.claude/rules.md §G.4](../.claude/rules.md) 强制）

- ✅ 模块代码完成 + 单测通过 + 提测 PR 评审 OK
- ✅ 后端编译 BUILD SUCCESS
- ✅ 字段对照表已在 [PRD-MAPPING.md §2](../PRD-MAPPING.md) 登记
- ✅ E2E spec 已建（至少 CRUD + 状态机合法/非法 + FK + 编码 HEX + UI 菜单可达性）

### 4.2 退出标准（Phase 04 → 05 准入）

- ✅ `npm run test:e2e:encoding` 6/6 通过（**P0 守门员**）
- ✅ `npm run test:e2e` 全套件 100% 通过
- ✅ DB HEX 全检查无 `EFBFBD` 字节
- ✅ P0/P1 缺陷为 0（[安全审计.md](安全审计.md) + [defect](../plm-frontend/e2e/defect.spec.ts) 输出）
- ✅ 通过证据落档 `99-跨阶段/gate-checklists/instances/<模块>/Phase03-Gate-*.md` §I

### 4.3 一票否决（**任何一项不允许跳过**）

- ❌ encoding 套件有 fail
- ❌ DB 任何字段 HEX 含 `EFBFBD`
- ❌ 后端启动未带 `-Dfile.encoding=UTF-8` 4 个标志

---

## 5. 风险

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 后端冷启动慢，导致 captcha 拿不到 | 测试 timeout | [E2E-运行手册.md §1](测试用例库/E2E-运行手册.md) "等 60s 出 Started PlmApplication" |
| Redis 用 `localhost` (IPv6 坑) | Lettuce 超时 | 强制用 `127.0.0.1`（[CLAUDE.md 4 个坑](../CLAUDE.md)） |
| Windows JDK 默认 GBK | 乱码 EFBFBD | 后端启动 4 个 `-D` 标志（[字符编码规范.md](../03-开发/字符编码规范.md)） |
| 测试间共享 captcha 5min 过期 | 部分 case fail | worker=1 串行 + 每个 spec 独立 login（已在 [auth.ts](../plm-frontend/e2e/helpers/auth.ts)） |
| 多人并发跑测试 | DB 数据互相冲突 | RUN_ID 后缀 + 每 spec 独立 cleanup（[fixtures.ts](../plm-frontend/e2e/helpers/fixtures.ts)） |
| db.ts 误用硬编码密码 | 凭据泄露 | DB_PASSWORD 环境变量；无默认值，未设置直接抛错（已修复 2026-05-17） |

---

## 6. 执行流程（按文档跑测试 SOP）

按 [E2E-运行手册.md §1 三分钟跑通](测试用例库/E2E-运行手册.md) 执行：

```bash
# 步骤 1 — 前置自检
netstat -ano | grep -E ":3306|:6379|:8081|:80"   # 4 端口都 LISTENING
echo "$DB_PASSWORD" | head -c 5                   # 非空
curl -s http://localhost:8081/captchaImage | head -c 100   # JSON

# 步骤 2 — 跑 P0 守门员
cd plm-frontend
npm run test:e2e:encoding                         # 期望 6 passed (~20s)

# 步骤 3 — 跑全套件
npm run test:e2e                                  # 期望 ~91 passed (~3min)

# 步骤 4 — 看结果
npm run test:e2e:report                           # HTML 报告
```

如果失败，按 [E2E-运行手册.md §4](测试用例库/E2E-运行手册.md) 排查表诊断。

---

## 7. CI 集成

参考 [E2E-运行手册.md §5 GitHub Actions](测试用例库/E2E-运行手册.md) 模板。当前 CI 触发：
- `push` 到任何分支 → 跑 `npm run test:e2e:smoke` (~15s)
- `push` 到 `main` / `claude/silly-lederberg-451a23` → 跑 `npm run test:e2e` 全套件
- PR 创建/更新 → 跑全套件 + 上传 playwright-report 到 artifact

---

## 8. 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-04-?? | 空模板 |
| v0.4 | 2026-05-17 | 首次填实质内容：13/13 PRD-aligned 模块覆盖映射、进入/退出标准、SOP、风险表、CI 集成 |
