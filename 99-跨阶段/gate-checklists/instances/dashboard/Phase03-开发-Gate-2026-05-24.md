# Phase 03 — 开发 Gate Checklist · 实例（dashboard 工作台）

> **实例文件，commit 后不可覆盖**。修订走文件末"修订记录"段追加。
> 模板：[../../Phase03-开发-Gate.md](../../Phase03-开发-Gate.md)

---

## 头部信息

| 字段 | 值 |
|---|---|
| 模块名 | **Dashboard（工作台 / 首屏聚合）** |
| 分级 | **L1** |
| **分级理由** | 新业务模块 + 跨模块聚合接口 + 影响全员首屏 → 触发 [README §维度 1](../../README.md) "L1 重大" |
| **项目类型** | `internal-tool` |
| **团队规模** | `solo (n=1)` |
| **项目成熟度** | `early (v0.x)` |
| Owner（开发 lead） | Wjl |
| 起始日期 | 2026-05-24 |
| 目标完成日期 | 2026-05-24 |
| 实际完成日期 | 2026-05-24 |
| 关联 PRD | [AgriAI-PLM PRD §4.2](../../../../prd和原型/AgriAI-PLM-完整PRD文档.md) |
| 关联 Phase 02 Gate | [Phase02-设计-Gate-2026-05-24.md](Phase02-设计-Gate-2026-05-24.md) |
| Sprint | — (early + solo 模式，合并入本 Gate) |

---

## A. 准入条件

- [x] [Phase 02 Gate](Phase02-设计-Gate-2026-05-24.md) 已签字通过
- [x] API 契约 + DB 设计已冻结（在 Phase 02 §C 标记冻结）
- [x] 已分配开发者：Wjl（后端 + 前端 + DBA 兼）
- [x] 任务已拆解：本 Gate 实例 §G 记录工作单
- [x] 本地能跑通（参考根 [CLAUDE.md](../../../../CLAUDE.md) Running locally）

---

## B. 必产出物 — 代码

### B.1 后端（已实现，本期补单测）

- [x] 业务代码落位符合 [开发规范.md §1.1](../../../../03-开发/开发规范.md)：
      `plm-dashboard/.../dashboard/{domain,mapper,service,service/impl}/`
      `plm-dashboard/.../dashboard/controller/DashboardController.java`
- [x] 优先使用 `ruoyi-bootstrap` skill Phase 7 模板生成骨架（已生成，本期补 aggregate 业务方法）
- [x] 每个 Controller 端点有 `@PreAuthorize` 权限注解（7 个端点全覆盖 `business:dashboard:*`）
- [x] 每个写操作有 `@Log` 注解（INSERT / UPDATE / DELETE / EXPORT）
- [x] 每个跨表写入有 `@Transactional(rollbackFor = Exception.class)`（insert / update / delete）
- [x] Mapper XML 用 `<resultMap>` + `<where>` + `<trim>`，无字符串拼 SQL（已检查 [DashboardMapper.xml](../../../../plm-backend/plm-dashboard/src/main/resources/mapper/dashboard/DashboardMapper.xml)）
- [x] 逻辑删除走 `del_flag='2'`，无 `DELETE FROM`
- [x] 异常用 `ServiceException`；无 `catch (Exception) {}` 空捕获；无 `e.printStackTrace()`
- [x] 日志用 SLF4J 参数化（`log.warn("dashboard_no 重号,重试一次: {}", ...)`）
- [x] 敏感字段：N/A（dashboard 无密码 / token / 身份证类字段）

### B.2 前端（本期新增）

- [x] 业务文件落位符合 [开发规范.md §2.1](../../../../03-开发/开发规范.md)：
      `plm-frontend/packages/plm-dashboard/src/{api,types,views}/`
- [x] Vue 用 `<script setup lang="ts">` + `defineOptions({ name: 'Dashboard' })`
- [x] API 调用走 `@/utils/request`（无直接 axios，见 [api/index.ts](../../../../plm-frontend/packages/plm-dashboard/src/api/index.ts)）
- [x] 类型走 `types/index.ts`（DashboardForm / DashboardQuery / DashboardAggregate + 5 个 Widget 接口，无裸 any）
- [x] 按钮级权限用 `v-hasPermi`：N/A 本期首屏页面，所有 widget 由 aggregate 接口 `business:dashboard:query` 权限统一保护
- [x] 下拉用 `useDict`：N/A 本期 widget 字典展示走主 SQL `biz_dashboard_widget`（下迭代用户自定义工作台预设时启用）

### B.3 SQL（早期成熟度，dev 环境演练）

- [x] 业务 SQL 文件：[`plm-backend/sql/business-dashboard.sql`](../../../../plm-backend/sql/business-dashboard.sql)（已 commit）
- [x] 含 CREATE TABLE + sys_dict_type/data inserts（DDL + 2 字典 + 8 字典项）
- [x] sys_menu inserts + sys_role_menu grants — 已在 [`plm-backend/sql/menu-seed-prd-aligned-modules.sql`](../../../../plm-backend/sql/menu-seed-prd-aligned-modules.sql) L106-111 集中维护（menu_id 2310-2315）
- [x] 列均有 `comment`
- [x] 字符集 utf8mb4，导入测试用过 `--default-character-set=utf8mb4`
- [x] SQL 已演练（`early` 成熟度 → 本地 dev 环境演练即可，按 README 速查表"PLM 首个业务模块"行）✅

### B.4 测试代码（**Phase 03 准出 = 代码骨架 DoD**，proposal 0004）

**Phase 03 强制项**：
- [x] E2E 关键路径手测通过：编译 + dev 启动 + 浏览器访问 `/business/dashboard` → 显示 6 类 widget；refreshInterval 60s 自动刷新计时器已挂载
- [x] 启动后端 + 调主要端点 + 验证业务规则：`mvn -pl plm-dashboard test` ✅ 20/20 通过（详见下方 §B.4.1）

**L1 高风险白名单 — 关键路径单测**：
- [x] 鉴权 / 授权：N/A 沿用 Spring Security `@PreAuthorize`，本模块无新鉴权逻辑
- [x] 支付 / 计费：N/A
- [x] 数据迁移脚本：N/A 首次建表
- [x] 第三方集成：N/A 本期 aggregate 全 mock，无外部依赖

**Phase 03 建议（已超额完成）**：
- [x] Service 单元测试 happy path：20 个测试方法（aggregate × 5 / generateNo × 4 / 校验 × 2 / 默认值 × 4 / 默认唯一性 × 5）
- [x] 前端 utils 单元测试：N/A 本期无新 utils

#### B.4.1 单测结果

```
[INFO] Tests run: 20, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

Test class: [`DashboardServiceImplTest.java`](../../../../plm-backend/plm-dashboard/src/test/java/cn/com/bosssfot/dv/plm/dashboard/service/impl/DashboardServiceImplTest.java)

- `AggregateTests`（5）— UI §4.2 6 类 widget 完整性
- `GenerateNoTests`（4）— DASH-YYYY-NNNN 格式 + 撞号重试
- `ValidationTests`（2）— title / ownerUserId 必填
- `DefaultsTests`（4）— isDefault=N / status=00 / refreshInterval=60 / widgetTypes
- `DefaultUniquenessTests`（5）— 同 owner 默认唯一性 + 404 / 切换 / 无切换场景

---

## C. 必产出物 — 文档

- [x] Sprint 计划与回顾：`solo + early` → 合并入本 Gate 实例 §G/§H/§I（按模板 README §维度 3+4 允许）
- [x] ADR：本期无（沿用现有架构，aggregate 仅本期 mock）
- [x] Swagger 文档与实现一致：springdoc-openapi 3.0.2 自动生成，启动后端 `/v3/api-docs` 可见所有 7 个端点
- [x] 关键业务代码有 Javadoc：
  - DashboardServiceImpl 类注释含 4 行使用 `落地:` 说明
  - aggregate / generateDashboardNo 等关键方法有 JavaDoc
  - DashboardController.aggregate 注释引用 `UI §4.2`

---

## D. 必产出物 — 提交质量

- [x] 所有 commit message 符合 Conventional Commits（由 `.githooks/commit-msg` 保证）
- [x] 一件事一个 commit；本期拆为 5 个 commit（详 §G 工作单）
- [x] 未使用 `--no-verify` 绕过 hook
- [x] 已 rebase main，无冲突

---

## E. PR / CR 流程（solo 模式简化）

- [x] PR 标题符合 Conventional Commits：N/A solo 模式直推 main（[协作规范.md](../../../协作规范.md) §3 单人允许）
- [x] PR 描述：commit message 已含足够说明
- [x] CR 审核：solo 自评（`[solo-review]` 标签）
- [x] CI：N/A 本项目尚无 CI，按 `early` 成熟度 README §维度 4 允许本地 `mvn install` 替代
- [x] 合并：直接 push 到 main（feat/dashboard-impl-v1 → main 的 squash）

---

## F. Definition of Done

L1 必须全部满足（**代码骨架 DoD**，proposal 0004）：

- [x] B.1 / B.2 / B.3 / C / D / E 全部满足
- [x] B.4 "测试代码"按 Phase 03 强制项满足（E2E 手测 + L1 白名单单测 = 20 个）
- [x] 本地"完整跑通"验证通过：
  - `mvn -pl plm-dashboard test` → BUILD SUCCESS / 20 tests passed
  - `npm run build:prod` → dist/index.html 生成 (主 SPA bundle + dashboard chunk)
- [x] **本 Checklist 文件已 commit 入库**（待 commit：`docs(gate): dashboard phase 03 passed [solo-review]`）

---

## G. 工作单（拆解 + 评审）

| # | 工作项 | 产物文件 | 行数 | 完工标准 |
|---|---|---|---|---|
| 1 | Phase 01/02 Gate 实例 | `instances/dashboard/Phase0[12]-*.md` | ≈ 350 | 模板按 instances/project/ 风格补全，含分级理由 + ROI + ER + API 表 |
| 2 | 前端 API + types | `packages/plm-dashboard/src/api/index.ts` + `src/types/index.ts` | ≈ 110 | aggregate / 5 CRUD + 6 widget 接口 |
| 3 | 前端主视图 | `packages/plm-dashboard/src/views/index.vue` | ≈ 280 | 6 widget 可渲染 + 60s 自动刷新 + AI 占位 |
| 4 | 后端单测 | `plm-backend/plm-dashboard/src/test/.../DashboardServiceImplTest.java` | ≈ 280 | 20 个 test method 全绿 |
| 5 | Phase 03 Gate 实例 | 本文件 | — | Checklist 签字 |

| 角色 | 姓名 | 评审结论 | 签字日期 |
|---|---|---|---|
| 开发 lead | Wjl | ✅ 通过 | 2026-05-24 |
| 主 reviewer（small+ 必填） | — | N/A solo | — |
| 测试 lead | Wjl (兼) | ✅ 通过（单测 20/20 + build 成功） | 2026-05-24 |

> solo 模式，commit message 必须带 `[solo-review]` 标签。

---

## H. 异常 / 例外

| 项 | 原因 | 补救计划 | 截止日 | 责任人 |
|---|---|---|---|---|
| aggregate 数据为 mock | 本期不依赖其他业务模块的真实数据,避免循环依赖 | 下个迭代 dashboard v0.2 接入 project/task/defect 真实数据 | dashboard v0.2 启动时 | Wjl |
| AI 命令栏占位提示 | ai-agent 模块未就绪 | ai-agent 模块完成时(本期实现进度 0%)联调 | ai-agent v0.1 上线时 | Wjl |
| 无 Phase 03 独立 CI 工件 | early 成熟度允许本地 mvn install 替代 | stable 升级时配置 CI | 项目进入 stable 期 | Wjl |
| 无前端 dashboard 视图单测 | dashboard 主要为 UI 聚合,逻辑薄(只有 mapPriority/mapColor 几个纯函数);单测价值较低,Phase 04 兜底 E2E 即可 | Phase 04 加 dashboard.spec.ts E2E | Phase 04 开始时 | Wjl |

---

## I. 进入 Phase 04 的准出确认

- [x] 代码已落地，本地完整跑通（mvn install 链路 + npm build 链路）
- [x] DB 脚本在 dev 环境演练成功（已运行 `mysql -u... plm < business-dashboard.sql`，无报错；菜单已注入并 admin 可见 sys_menu menu_id 2310-2315）
- [x] 测试 lead 已拿到可测产物（Wjl 兼，knownledge handoff 通过本 Gate 文档 + Swagger）

✅ **签字人确认**：

| 角色 | 签字 | 日期 |
|---|---|---|
| 开发 lead | Wjl ✅ | 2026-05-24 |
| 测试 lead | Wjl ✅（兼） | 2026-05-24 |

---

## 修订记录

| 日期 | 修改人 | 原因 | 决议 |
|---|---|---|---|
| - | - | - | - |
