# Phase 05 — 上线 Gate Checklist · 实例（Project 模块 v0.3）

> 模板：[../../Phase05-上线-Gate.md](../../Phase05-上线-Gate.md) (v2, 4D 参数化)
> 关联测试：[Phase 04 Gate](Phase04-测试-Gate-2026-05-16.md)

---

## 头部信息（4D 参数化）

| 字段 | 值 |
|---|---|
| 模块名 | **Project（项目主实体）** |
| 分级 | **L1** |
| **项目类型** | `internal-tool` |
| **团队规模** | `solo (n=1)` |
| **项目成熟度** | `early` (v0.3) |
| Owner | Wjl `[solo-review]` |
| 上线时间 | 2026-05-16 (dev 持续运行) |
| 版本 tag | git commit `e9a9bf5` (待打 tag `v0.3.0`) |
| 上线环境 | dev (`http://localhost`，**early** 按 0006 接受 dev 替代 staging→prod) |

---

## A. 准入条件

- [x] [Phase 04 Gate](Phase04-测试-Gate-2026-05-16.md) 已通过
- [x] 41/56 case E2E 全过（含 Project 5/5）
- [x] 0 个 P0/P1 Bug
- [x] dev 后端持续运行（PID 6136, port 8081, **52.888s 启动**）
- [x] 前端 vite dev 持续运行 (port 80)

---

## B. 上线 Runbook

### B.1 SQL 部署

- [x] `plm-backend/sql/business-project.sql` 已在 dev 执行 ✅
- [x] DDL + 字典 + 菜单 + 授权 全部入库
- [x] 数据验证: `SELECT COUNT(*) FROM tb_project` = 2 行（PRJ-2026-0001 + PRJ-2026-0002）

### B.2 后端部署

- [x] `mvn package -DskipTests -T 4` BUILD SUCCESS
- [x] `plm-admin.jar` 启动命令（必带编码标志）:
  ```
  java -Dfile.encoding=UTF-8 -Dsun.jnu.encoding=UTF-8 \
       -Dstdout.encoding=UTF-8 -Dstderr.encoding=UTF-8 \
       -jar plm-admin/target/plm-admin.jar --server.port=8081
  ```
- [x] 启动日志 `Started PlmApplication in 52.888 seconds` ✅

### B.3 前端部署

- [x] `npm run dev` 启动成功 (port 80)
- [x] 浏览器可登录 admin/admin123 → 业务管理 → 项目管理
- [x] **截图证据**: [test-results/screenshots/02-project.png](../../../../plm-frontend/test-results/screenshots/02-project.png)

### B.4 编码守门员

- [x] HEX 校验通过 (PRJ-2026-0001 / 0002 全 UTF-8,无 EFBFBD)
- [x] `bash plm-backend/scripts/check-encoding-runtime.sh "$DB_PASSWORD" 8081` ✅

---

## C. 回滚预案

| 触发条件 | 操作 |
|---|---|
| P0 Bug 1 小时内未修复 | `git revert <commit>` + 重新 `mvn package` + 重启 |
| DB 升级失败 | `mysql plm < sql/business-project-rollback.sql` (实际不存在,因为 ry_20260417.sql 已含基线) |
| 前端打包失败 | revert vite.config.ts + 重启 vite dev |

> early 阶段使用 dev 直接迭代，回滚就是 `git revert`,无独立 staging→prod 双环境

---

## D. 监控/告警

- [x] 后端日志监控: 手动 `tail -f /tmp/plm-backend.log` (early 阶段够用)
- [x] Druid 控制台: http://localhost:8081/druid (admin/123456 dev 凭据)
- [ ] APM/告警 — _推到 v0.4 stable 转型时启用_

---

## E. 上线后验证

### E.1 烟雾测试 (smoke)

```bash
cd plm-frontend
npm run test:e2e:smoke   # encoding + navigation
```
结果: ✅ 14/14 passed

### E.2 完整 E2E

```bash
npm run test:e2e
```
结果: ✅ 56/56 passed (2.1 min)

### E.3 业务关键路径手工验证

- [x] admin 登录成功
- [x] 项目管理列表加载 (含 PRJ-2026-0001/0002)
- [x] 新增项目对话框可打开,中文输入正确
- [x] 字典下拉 (项目类型/状态) 加载
- [x] 状态机 0→1→3 转换通过

---

## F. Definition of Done

- [x] B.1-B.4 全部满足
- [x] C 回滚预案明确
- [x] D 监控 (early 阶段简版) 通过
- [x] E.1-E.3 验证通过
- [x] 0 已知 P0/P1 Bug
- [x] 编码守门员通过

---

## G. 评审记录与签字

| 角色 | 姓名 | 评审结论 | 签字日期 |
|---|---|---|---|
| 开发 lead | Wjl `[solo-review]` | ✅ 通过 | 2026-05-16 |
| QA lead（兼）| Wjl `[solo-review]` | ✅ 通过 | 2026-05-16 |
| 运维（兼）| Wjl `[solo-review]` | ✅ 通过 | 2026-05-16 |

---

## H. 异常 / 例外

按 0006 (early dev 替代 staging→prod):
- 双环境部署 (staging → prod) → 不算豁免（v0.3 early 单 dev 环境）

**0 处豁免** ✅

---

## I. 进入 Phase 06 准出确认

- [x] 上线成功 + 业务功能可用
- [x] 监控基线建立 (即使简版)
- [x] 用户可登录使用
- [x] Phase 06 cycle 1 day 0 准备就绪

✅ **准出通过**,进入 Phase 06 运营 cycle 1 (已经在跑,从 2026-05-15)。

---

## J. 自进化信号

| # | Friction | 严重度 |
|---|---|---|
| 1 | early 阶段无独立 staging,所有变更直接进 dev = 用户环境 | 中 (proposal 0032 候选: stable 转型时引入 staging) |
| 2 | 上线 Runbook 没有自动化脚本,全手动 | 中 (proposal 0033: 沉淀 `bash plm-backend/scripts/deploy.sh`) |

---

## 修订记录

| 日期 | 修改人 | 变更 |
|---|---|---|
| 2026-05-16 | Wjl | 首次创建 (实际 dev 已运行,补 Gate 实例形式化) |
