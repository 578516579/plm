# Phase 03 — 开发 Gate Checklist · 实例（Task 模块）

> **实例文件，commit 后不可覆盖**。
> 模板：[../../Phase03-开发-Gate.md](../../Phase03-开发-Gate.md) (v2, 4D 参数化)
> 关联设计：[Phase 02 Gate](Phase02-设计-Gate-2026-05-16.md) v1.0

---

## 头部信息（4D 参数化）

| 字段 | 值 |
|---|---|
| 模块名 | **Task（开发任务 + 看板）** |
| 分级 | **L1** |
| **分级理由** | 新业务模块 + DB 结构性变更（`tb_task` + 2 字典 + 8 菜单 含"我的任务"一级菜单） + 8 端点（含 kanban + my-tasks 独有） + **6×6 状态机含反向边（02 ↔ 01 评审打回，03 → 02 测试打回）** + 3 个 FK（Project + Requirement + Sprint） |
| **项目类型** | `internal-tool` |
| **团队规模** | `solo (n=1)` |
| **项目成熟度** | `early` (v0.2) |
| Owner | Wjl |
| 实际完成日期 | 2026-05-16 |
| 关联 PRD | [Task-PRD.md](../../../../01-立项/Task-PRD.md) v1.0 |
| 关联设计 | [DB](../../../../02-设计/Task-数据库设计.md) + [API](../../../../02-设计/Task-API设计.md) v1.0 |

---

## A. 准入条件

- [x] [Phase 02 Gate](Phase02-设计-Gate-2026-05-16.md) 已签字通过
- [x] API 契约 + DB 设计已冻结（8 端点 + 17 字段 + 6×6 状态机 + 7 错误码）
- [x] 已分配开发者（Wjl solo）
- [x] **依赖兄弟模块**：FK 校验需调 ProjectMapper / RequirementMapper / SprintMapper 的 `selectById` — 三个 Mapper 已在 Project P03（2026-05-15）+ Requirement/Sprint P03（本期，2026-05-16）落地

---

## B. 必产出物 — 代码

### B.1 后端（9 个文件）

- [x] `plm-system/.../business/domain/Task.java`（17 字段：3 FK + title + description + status + priority + assignee + 2 hours + 2 mr_* + 6 通用）
- [x] `plm-system/.../business/mapper/TaskMapper.java`（10 方法含 `countBySprintId` / `countByStatusAndSprint` / `selectKanbanTasks` / `selectBySprintId`）
- [x] `plm-system/.../mapper/business/TaskMapper.xml`（resultMap + 9 SQL，含**看板 SQL** `status != '05' AND project_id=? AND (sprint_id=? OR null)`）
- [x] `plm-system/.../business/service/ITaskService.java`（7 方法含 `selectMyTasks` + `kanban`）
- [x] `plm-system/.../business/service/impl/TaskServiceImpl.java`：
  - **6×6 STATUS_TRANSITIONS 矩阵含反向边**（02→01 评审打回，03→02 测试打回）
  - generateTaskNo（ADR-0003：TASK-YYYY-NNNN）+ DuplicateKey 重试
  - **3 FK 校验**：projectId（必填）+ requirementId（可空）+ sprintId（可空）
  - **MR URL 格式校验**（http(s):// 正则）
  - **进入「已完成」必填 actualHours**（错误码 602）
  - `selectMyTasks` 注入当前 `SecurityUtils.getUserId()`
  - `kanban` 服务层按状态分组 + 每列 LIMIT 50 内存切片
- [x] `plm-admin/.../web/controller/business/TaskController.java`（8 端点）
  - 含 `/my`（复用 list 权限）+ `/kanban?projectId=&sprintId=`（独有 kanban 权限）
- [x] 跨表写入有 `@Transactional(rollbackFor = Exception.class)`
- [x] 异常用 `ServiceException`（404 / 601 / 602 / 604 / 701 / 702）

### B.2 前端（5 个文件）

- [x] `src/api/business/task.ts`（7 接口含 myTasks + kanbanTasks）
- [x] `src/types/api/business/task.ts`（TaskForm + TaskQuery + **KanbanColumn + KanbanView** 看板类型）
- [x] `src/views/business/task/index.vue`（标准 CRUD + 3 FK 字段 + MR 字段 + 工时字段）
- [x] `src/views/business/task/kanban.vue`（**看板视图 only-read** Flex 横向布局 + 5 列 + 卡片样式）
- [x] `src/views/business/task/my.vue`（**我的任务** 简化列表 + MR 直链）
- [x] 按钮级权限用 `v-hasPermi`

### B.3 SQL（2 个文件）

- [x] `plm-backend/sql/business-task.sql`：CREATE TABLE + 2 字典类型 + 9 字典数据 + 8 菜单 + admin 授权
- [x] `plm-backend/sql/business-task-rollback.sql`
- [x] 含 6 个索引（**`(status, priority)` 复合索引**用于看板列分组 + P0/P1 紧急查询）
- [ ] SQL 在 staging 演练 — **0006 合规**

### B.4 测试代码

- [ ] Service 单元测试 — **0004 合规**：Phase 03 仅强制 E2E
- [x] **6×6 状态机单测必要性**：Phase 04 必须覆盖 36 case，特别**反向边**（02↔01 + 03→02）
- [x] **kanban EXPLAIN 验证必要性**：Phase 04 必须验证 `idx_task_sprint` 走对
- [x] **FK 校验测试**：Phase 04 测三个 FK 不存在的 702 case

---

## C. 必产出物 — 文档

- [x] 设计文档 + ServiceImpl 顶部 Javadoc 完整（含 6×6 矩阵图 + 反向边语义说明）
- [ ] Sprint 计划/回顾 — **0005 合规**
- [x] ADR-0003 草案 / TASK-YYYY-NNNN 编号规则在 Service Javadoc 显式声明

---

## D. 必产出物 — 提交质量

- [x] commit message 符合 Conventional Commits
- [x] **未使用 `--no-verify` 绕过 hook**
- [x] **`mvn install -DskipTests`** BUILD SUCCESS ✅

---

## E. PR / CR 流程

- [x] solo 模式自审 + commit 直接进 main
- [x] commit 描述含：背景 / 改动点 / 自测 / 关联文档
- [x] CI 全绿（本地编译代替）

---

## F. Definition of Done

L1 全部满足（按 0004/0005/0006 重判）：

- [x] B.1 / B.2 / B.3 已完成
- [x] B.4 — 0004 staged DoD 合规
- [x] C 文档完整 — 0005 Sprint 文档合并合规
- [x] D 提交规范全过
- [x] E 自审已过
- [x] **mvn install BUILD SUCCESS** ✅
- [x] **本 Checklist 已 commit 入库**

---

## G. 评审记录与签字

| 角色 | 姓名 | 评审结论 | 签字日期 |
|---|---|---|---|
| 开发 lead | Wjl `[solo-review]` | ✅ 通过 | 2026-05-16 |

---

## H. 异常 / 例外

按 0004/0005/0006 合入后重判，**0 处豁免** ✅

---

## I. 进入 Phase 04 的准出确认

- [x] 代码已落位且 mvn install BUILD SUCCESS
- [x] DB 脚本 dev 演练挂账 Phase 04
- [x] 测试 lead 已拿到可测产物（8 端点 + 看板 + 我的任务）
- [x] **Phase 04 必测清单**：
  - 6×6 状态机 36 case 全覆盖（含反向边 02↔01 + 03→02）
  - 进入「已完成」必填 actualHours
  - MR URL 格式校验
  - 3 个 FK 校验 702 case
  - kanban EXPLAIN 验证（`idx_task_sprint` 走对）
  - 看板视图性能要求 < 800ms（PRD §4）

✅ 准出通过。

---

## J. 自进化信号

| # | Friction | 严重度 | 建议 |
|---|---|---|---|
| 1 | MR URL 格式校验用简单正则 `^https?://.+`，不验证 host 合法性 — 可能漏过 `http://test.evil/foo` 类钓鱼链接 | 低 | proposal 0025（候选）：MR URL 加白名单 host（gitlab/github/bitbucket）— 进入 staging 阶段再决议 |
| 2 | 三个 FK 校验是顺序 if/else 分别调 mapper，三次 SQL — 可合并 | 低 | 当前 Project 数量极小，三次 SQL 0.5ms，性能可忽略 |
| 3 | `selectMyTasks` 用 `SecurityUtils.getUserId()` 覆盖了入参的 `assigneeUserId`，如果用户传了别人的 userId 会被静默改写为自己的 — 行为正确但日志没记录 | 低 | 加 debug 日志便于排查 |
| 4 | kanban 5 列固定 hard-code（"00 待开发" → "04 已完成"），新增状态时需要同时改 SQL + Service + 前端 | 中 | proposal 0026（候选）：看板列从字典 `biz_task_status` 读取（排除终态 `04 / 05` 中的 `05`），Phase 04 决议 |

→ 追加 [signals/2026-05.md](../../../../99-跨阶段/signals/2026-05.md)。

---

## 修订记录

| 日期 | 修改人 | 变更 |
|---|---|---|
| 2026-05-16 | Wjl | 首次创建 |
