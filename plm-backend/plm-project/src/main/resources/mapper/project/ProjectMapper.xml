<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.bosssfot.dv.plm.project.mapper.ProjectMapper">

    <resultMap type="Project" id="ProjectResult">
        <result property="id"              column="id"               />
        <result property="projectNo"       column="project_no"       />
        <result property="projectName"     column="project_name"     />
        <result property="businessLine"    column="business_line"    />
        <result property="projectType"     column="project_type"     />
        <result property="priority"        column="priority"         />
        <result property="lifecyclePhase"  column="lifecycle_phase"  />
        <result property="status"          column="status"           />
        <result property="progress"        column="progress"         />
        <result property="health"          column="health"           />
        <result property="managerUserId"   column="manager_user_id"  />
        <result property="startDate"       column="start_date"       />
        <result property="endDate"         column="end_date"         />
        <result property="description"     column="description"      />
        <result property="createBy"        column="create_by"        />
        <result property="createTime"      column="create_time"      />
        <result property="updateBy"        column="update_by"        />
        <result property="updateTime"      column="update_time"      />
        <result property="remark"          column="remark"           />
        <result property="delFlag"         column="del_flag"         />
    </resultMap>

    <sql id="selectProjectVo">
        select id, project_no, project_name, business_line, project_type, priority,
               lifecycle_phase, status, progress, health, manager_user_id,
               start_date, end_date, description,
               create_by, create_time, update_by, update_time, remark, del_flag
        from tb_project
    </sql>

    <select id="selectProjectList" parameterType="Project" resultMap="ProjectResult">
        <include refid="selectProjectVo"/>
        <where>
            del_flag = '0'
            <if test="projectNo != null and projectNo != ''">
                and project_no like concat('%', #{projectNo}, '%')
            </if>
            <if test="projectName != null and projectName != ''">
                and project_name like concat('%', #{projectName}, '%')
            </if>
            <if test="businessLine != null and businessLine != ''">
                and business_line = #{businessLine}
            </if>
            <if test="projectType != null and projectType != ''">
                and project_type = #{projectType}
            </if>
            <if test="priority != null and priority != ''">
                and priority = #{priority}
            </if>
            <if test="lifecyclePhase != null and lifecyclePhase != ''">
                and lifecycle_phase = #{lifecyclePhase}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
            <if test="health != null and health != ''">
                and health = #{health}
            </if>
            <if test="managerUserId != null">
                and manager_user_id = #{managerUserId}
            </if>
            <if test="params.beginStartDate != null and params.beginStartDate != ''">
                and start_date &gt;= #{params.beginStartDate}
            </if>
            <if test="params.endStartDate != null and params.endStartDate != ''">
                and start_date &lt;= #{params.endStartDate}
            </if>
        </where>
        order by create_time desc
    </select>

    <select id="selectProjectById" parameterType="Long" resultMap="ProjectResult">
        <include refid="selectProjectVo"/>
        where id = #{id} and del_flag = '0'
    </select>

    <insert id="insertProject" parameterType="Project" useGeneratedKeys="true" keyProperty="id">
        insert into tb_project
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="projectNo != null">project_no,</if>
            <if test="projectName != null">project_name,</if>
            <if test="businessLine != null">business_line,</if>
            <if test="projectType != null">project_type,</if>
            <if test="priority != null">priority,</if>
            <if test="lifecyclePhase != null">lifecycle_phase,</if>
            <if test="status != null">status,</if>
            <if test="progress != null">progress,</if>
            <if test="health != null">health,</if>
            <if test="managerUserId != null">manager_user_id,</if>
            <if test="startDate != null">start_date,</if>
            <if test="endDate != null">end_date,</if>
            <if test="description != null">description,</if>
            <if test="createBy != null">create_by,</if>
            <if test="remark != null">remark,</if>
            create_time
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="projectNo != null">#{projectNo},</if>
            <if test="projectName != null">#{projectName},</if>
            <if test="businessLine != null">#{businessLine},</if>
            <if test="projectType != null">#{projectType},</if>
            <if test="priority != null">#{priority},</if>
            <if test="lifecyclePhase != null">#{lifecyclePhase},</if>
            <if test="status != null">#{status},</if>
            <if test="progress != null">#{progress},</if>
            <if test="health != null">#{health},</if>
            <if test="managerUserId != null">#{managerUserId},</if>
            <if test="startDate != null">#{startDate},</if>
            <if test="endDate != null">#{endDate},</if>
            <if test="description != null">#{description},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="remark != null">#{remark},</if>
            sysdate()
        </trim>
    </insert>

    <update id="updateProject" parameterType="Project">
        update tb_project
        <trim prefix="SET" suffixOverrides=",">
            <if test="projectNo != null">project_no = #{projectNo},</if>
            <if test="projectName != null">project_name = #{projectName},</if>
            <if test="businessLine != null">business_line = #{businessLine},</if>
            <if test="projectType != null">project_type = #{projectType},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="lifecyclePhase != null">lifecycle_phase = #{lifecyclePhase},</if>
            <if test="status != null">status = #{status},</if>
            <if test="progress != null">progress = #{progress},</if>
            <if test="health != null">health = #{health},</if>
            <if test="managerUserId != null">manager_user_id = #{managerUserId},</if>
            <if test="startDate != null">start_date = #{startDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
            <if test="description != null">description = #{description},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </trim>
        where id = #{id}
    </update>

    <update id="deleteProjectByIds" parameterType="Long">
        update tb_project set del_flag = '2' where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- ADR-0001: 查当年内最大流水号(PRJ-YYYY-NNNN 末 4 位) -->
    <select id="selectMaxSeqOfYear" parameterType="String" resultType="java.lang.Integer">
        select max(cast(substring(project_no, length(#{prefix}) + 1) as unsigned)) as max_seq
        from tb_project
        where project_no like concat(#{prefix}, '%')
    </select>

</mapper>
